{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "module": "ESNext", // ← ESM for tests
    "moduleResolution": "bundler", // ← or "node16"
    "types": ["vitest/globals", "node"]
  },
  "include": ["src/**/*.test.ts", "src/**/*.spec.ts"]
}
