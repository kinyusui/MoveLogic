{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "module": "nodenext", // ← ESM for tests
    "moduleResolution": "nodenext", // ← or "node16"
    "types": ["vitest/globals", "node"]
  },
  "include": ["src/**/*.test.ts", "src/**/*.spec.ts"],
  "exclude": ["node_modules", "**/node_modules/*"]
}
